{
  "name": "setup-deck",
  "version": "1.0.0",
  "description": "Make decK available in your GitHub Actions workflows",
  "main": "dist/index.js",
  "scripts": {
    "test": "jest",
    "build": "npx @vercel/ncc build && npx convert-action"
  },
  "keywords": [
    "kong",
    "deck",
    "github",
    "actions"
  ],
  "author": "Michael Heap <m@michaelheap.com>",
  "license": "Apache-2.0",
  "dependencies": {
    "@actions/core": "^1.4.0",
    "@actions/tool-cache": "^1.7.0",
    "semver": "^7.3.5"
  },
  "devDependencies": {
    "jest": "^27.0.3"
  }
}
